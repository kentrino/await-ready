{
  "name": "await-ready",
  "version": "0.1.1",
  "description": "Check if a service is ready",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kentrino/await-ready"
  },
  "bin": {
    "await-ready": "./dist/main.mjs"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.mts",
        "default": "./dist/index.mjs"
      }
    }
  },
  "scripts": {
    "dev": "bun run src/main.ts",
    "build": "tsdown",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check .",
    "test": "vitest run",
    "knip": "knip-bun",
    "check": "bun run lint && bun run fmt:check && bun run knip",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "citty": "^0.2.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "knip": "^5.83.1",
    "oxfmt": "^0.28.0",
    "oxlint": "^1.43.0",
    "tsdown": "^0.20.3",
    "vitest": "^4.0.18"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
